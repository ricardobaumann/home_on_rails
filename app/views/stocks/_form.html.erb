<%= form_for(@stock) do |f| %>
  <% if @stock.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stock.errors.count, "error") %> prohibited this stock from being saved:</h2>

      <ul>
      <% @stock.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :entry_date %><br>
    <%= f.datetime_select :entry_date %>
  </div>
  <div class="field">
    <%= f.label :stock_operation %><br />
    <%= f.select :stock_operation_id, options_for_select(StockOperation.all.map{|s|[s.name, s.id]}, @stock.stock_operation_id) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
